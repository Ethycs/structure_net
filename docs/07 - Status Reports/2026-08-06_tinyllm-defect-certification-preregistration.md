# TinyLLM defect certification preregistration

**Status:** PREREGISTERED — certification attempted before interpretation  
**Date:** 2026-08-06  
**Hypothesis:** `tinyllm-d6-step15-defect-certificate-v1`

## Question

Can the d6 step `14 -> 15` degree-changing cylinder be upgraded from a charged
grid cell to a computer-assisted certificate of one unique, regular, positively
indexed zero of the declared continuous TinyLLM lift?

## Fixed object

Deterministically replay the retained seed-7 d6 phase task with its original
data and minibatches. Verify the 600-step final state digest against the source
campaign, then retain exact step-14 and step-15 states. Use their straight-line
weight interpolation, the adjacent-token-embedding continuous input lift, and
the previously localized rectangle, expanded only before any certificate
outcome is inspected if its boundary is unresolved.

## Certification gate

The full claim passes only if all of the following are machine-checked with
outward-rounded enclosures that cover the actual network map and its first
derivatives—not merely an interpolant at sampled points:

1. the complete cylinder boundary excludes zero;
2. interval Newton or Krawczyk maps one rectangle strictly into itself;
3. every other rectangle is certified root-free;
4. the determinant interval for the actual network Jacobian is strictly
   positive;
5. endpoint phase-loop windings are resolved and differ by `+1`.

Dense sampling, a converged floating-point root, a Chebyshev fit, or an
empirical remainder audit is secondary evidence and cannot satisfy this gate
without a rigorous network-to-surrogate remainder enclosure.

## Outcomes

| Outcome | Verdict |
| --- | --- |
| all enclosure gates pass | one unique regular `+1` defect is computer-assisted certified on the declared lift |
| surrogate certifies but network remainder is not rigorous | certification fails; retain a validated-surrogate localization only |
| determinant contains zero | regularity and index are not certified |
| multiple candidate boxes remain | uniqueness is not certified |

## Artifacts

The append-only root is
`data/experiments/tinyllm_defect_certification/20260806_d6_step15`, retaining
endpoint digests, root boxes, all interval/surrogate coefficients and bounds,
boundary audits, and `results.json`.
